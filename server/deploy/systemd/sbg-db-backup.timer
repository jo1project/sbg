# 每天 04:30(VPS 時區 Asia/Taipei)備份一次;關機錯過時開機後補跑(Persistent)。
[Unit]
Description=Daily snake battle server SQLite backup

[Timer]
OnCalendar=*-*-* 04:30:00
Persistent=true
RandomizedDelaySec=5min

[Install]
WantedBy=timers.target
