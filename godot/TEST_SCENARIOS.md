# 手動測試情境

依 `docs/snake-battle-spec.md`（下稱「規格」）逐條寫的手動測試。每條規則至少一個情境；情境編號對應 `godot/PARITY.md` 的「驗證方式」欄。
同一份情境 Flutter 版和 Godot 版都要跑，結果要一樣——Flutter 的結果就是 Godot 的標準答案（兩者不同時先看 PARITY.md 文末「規格 vs Flutter 差異」）。

## 共通準備

1. **伺服器開除錯模式**（只在開發機！）：
   ```bash
   cd server && SBG_DEBUG_COMMANDS=1 npm start
   ```
   啟動訊息要出現「開發用除錯指令已啟用」。
2. **client 連本機伺服器**：Flutter 用 `--dart-define=SERVER_URL=ws://<電腦IP>:8080`；Godot 之後接上伺服器時用同一個網址。
3. **除錯面板**：Godot debug build 左上角「DBG」或按 F3。伺服器欄填同一個網址 → 連線 → 「重新整理」→ 目標玩家選要測的那位（面板上的指令都是針對這位玩家/他的房間/他的 NPC 對手）。
4. 下文的「**NPC 對局**」= 按隨機配對，等 8 秒自動配到 NPC。「**安靜 NPC**」= NPC 對局後在面板取消勾選「NPC 自動攻擊」，避免 NPC 自己亂打干擾時序（NPC 還是會自己吃食物加能量）。
5. 下文的除錯指令寫法：`能量(10)` = 面板「設定能量」填 10 按設定；`下次效果(暫停)`；`NPC閃避(永遠/永遠不/正常)`；`NPC攻擊(類型, 延遲ms, 能量)`；`NPC自動攻擊(關)`。
6. 需要兩個真人的情境用兩台裝置（或一台手機 + 一個電腦上的 client）。

數值（伺服器 `events.js` CONFIG）：能量上限 10、閃躲視窗 1000ms、預告 1000ms、防騷擾 3 次 / 暫停 3000ms、暫停效果上限 3000ms、食物 3 個、NPC 閃避 30%、雙殺窗口 200ms、NPC 補位 8000ms、邀請 30000ms、斷線寬限 10000ms、小地圖 2000ms。
移動：一般 325ms/格、加速 160ms/格；開局 4 節。

---

## C. 連線與帳號（規格 7.4、8.1）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| C-01 | 7.4 identify | 清掉 App 資料（第一次安裝） | 開 App | 大廳顯示「你的ID:XXXXXX」；伺服器 log 有 identify 的 deviceInfo |
| C-02 | 8.1 ID 格式、本地保存 | C-01 之後 | 看 ID；關掉 App 再開 | ID 是 6 碼英數大寫、沒有 0/O/1/I；重開後 ID 不變 |
| C-03 | 8.1 還原碼只給一次 | 清資料後第一次開 | 看到還原碼畫面，按「我已保存」；關 App 再開 | 第一次出現「請截圖保存你的還原碼」（格式 xxxx-xxxx-xxxx）；之後再開不會再出現 |
| C-04 | 8.1 還原帳號 | 記下 C-03 的 ID 與還原碼；清 App 資料 | 在還原畫面輸入還原碼 | 回到原本的 ID。輸入不存在的還原碼 → 「還原碼查無資料」 |
| C-05 | 7.4 心跳 | 面板連線 | 對戰中看伺服器 log／面板 `debug_list` | client 每秒送 ping，伺服器回 pong（RTT 用於閃躲校正，見 D-03） |
| C-06 | 連線失敗 | 伺服器關掉 | 開 App | 顯示「連線失敗,請檢查網路連線」＋重新連線按鈕；開伺服器後按重新連線可進大廳 |
| C-07 | App 切背景 | NPC 對局中 | 把 App 切到背景 | client 送 leave_room，伺服器結束房間；回到 App 可重新配對（不會卡 self_busy） |

## M. 配對（規格 8.2、8.3）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| M-01 | 8.2 隨機配對真人 | 兩台裝置 A、B | A、B 在 8 秒內都按隨機配對 | 兩邊都進入對戰，對手 ID 是對方 |
| M-02 | 8.2 等待中取消 | — | 按隨機配對，在「配對中...」按取消 | 回大廳；8 秒後不會被配 NPC |
| M-03 | 8.2 NPC 補位 | 只有一台裝置 | 按隨機配對，計時 | 約 8 秒後進入對戰，面板 `debug_list` 看到對手是 `npc_xxxxxx` |
| M-04 | 8.3 好友不在線 | — | 邀請一個不存在/離線的 ID | 「邀請失敗:target_offline」 |
| M-05 | 8.3 對方忙碌 | B 正在 NPC 對局 | A 邀請 B | 「邀請失敗:target_busy」，B 沒收到任何通知 |
| M-06 | 8.3 邀請 → 接受 | A、B 都在大廳 | A 輸入 B 的 ID 邀請；B 按接受 | B 看到「A 邀請你對戰」；接受後雙方進入同一場對戰 |
| M-07 | 8.3 拒絕 | 同上 | B 按拒絕 | A 看到「對方拒絕了邀請」，不附理由；A 回大廳 |
| M-08 | 8.3 30 秒逾時 | 同上 | B 不回應，等 30 秒 | A 看到「邀請逾時未回應」；B 的邀請視窗消失 |
| M-09 | 8.3 等待中鎖定 | A 已送出邀請等待中 | A 試著再邀請別人 / 按隨機配對 | 等待畫面只有「取消」可按；伺服器若收到會回 self_busy / busy |
| M-10 | 8.3 撤回 | A 已送出邀請 | A 按取消 | B 看到「對方已撤回邀請」，邀請視窗消失 |
| M-11 | 連線記錄 | 打過幾場 | 回大廳看「連線記錄」 | 最近的對手在最前面，最多 10 筆，點一下可直接邀請 |
| M-12 | 2.3 開局資料 | NPC 對局 | 進入對戰畫面 | 地圖、3 顆食物在倒數時就已經畫好（不會空白） |
| M-13 | 開局倒數 | NPC 對局 | 看開局 | Flutter：3-2-1 倒數後蛇自動往右走；倒數中不能動（Godot 目前是等第一次輸入，見 PARITY 差異 #8） |

## G. 地圖（規格 2.2～2.5、7.7）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| G-01 | 2.3 各自抽地圖 | 真人 A、B 對戰（多打幾場） | 比對雙方畫面 | 雙方地圖可能不同（map_01/02/03），整場不變 |
| G-02 | 2.5 地板變化 | — | 看地板 | 大多是 floor_1，約 1 成是裂痕/苔蘚；同一格每次都一樣 |
| G-03 | 2.4、7.7 障礙物種類 | 抽到 map_01/02 | 看障礙物 | crate、column、chest、四種小怪物（goblin/skelet/imp/chort）都正確顯示；怪物持續播放待機動畫 |
| G-04 | 7.7 大型怪物兩格 | 抽到 map_03 | 看 big_demon/big_zombie/ogre；分別撞左格、右格 | 怪物跨兩格置中顯示；撞 (x,y) 或 (x+1,y) 都死亡 obstacle；食物不會出現在這兩格 |
| G-05 | 2.4 走廊淨空、火把 | 抽到 map_01 | 看走廊 | 走廊沒有障礙物；房間左右邊緣有火把、石柱頂上有火把 |

## S. 移動與死亡（規格 2.2、2.3、6、6.1）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| S-01 | 移動速度 | NPC 對局、安靜 NPC | 直走 10 格計時 | 約 3.25 秒（325ms/格） |
| S-02 | 開局位置 | NPC 對局 | 看開局 | 蛇頭在地圖 spawnPos，4 節往左排，面向右 |
| S-03 | 轉向 | — | 搖桿往上/下/左/右（含斜角 44° 與 46°） | 依角度切成 4 方向（右 [-45°,45°)、下 [45°,135°)、上 [-135°,-45°)、其餘左）；手指移動 < 10dp 不轉向 |
| S-04 | 不能迴轉 | 蛇往右走 | 搖桿往左 | 忽略，繼續往右 |
| S-05 | 2.2 撞邊界 | NPC 對局 | 直直走出房間/地圖 | 死亡（wall），game_over：你輸了（single_death） |
| S-06 | 2.3 撞虛空 | 抽到 map_01 | 從房間側邊走進黑色虛空（不是走廊） | 死亡（wall） |
| S-07 | 6 撞自己 | 被直接攻擊變長到 ≥5 節（用 `NPC攻擊(direct, 0, 5)`） | 繞小圈撞自己 | 死亡（self）；只有 4 節時繞 2x2 小圈**不會**死（尾巴那格會讓開） |
| S-08 | 2.3 撞障礙物 | — | 走進障礙物格 | 死亡（obstacle） |
| S-09 | 6.1 死亡回報被伺服器採信 | 同 S-05 | 死亡 | 伺服器 log 沒有「碰撞驗證失敗」；對手收到你輸了的另一面（你贏了） |
| S-10 | 6.1 假死亡被拒 | 用腳本（WebSocket）以玩家身分送 `death_report {cause:"wall", headPos:{x:5,y:5}}`（地圖中央） | — | 伺服器回 death_report_rejected（collision_not_verified），不判定勝負；Flutter 收到會顯示「死亡回報未通過伺服器驗證,已返回大廳」 |
| S-11 | 7.3 座標同步 | NPC 對局 | 看伺服器 log / 抓封包 | client 每 1 秒送 snake_position_update（headPos、bodyCells） |

## F. 食物（規格 5）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| F-01 | 5 恆定 3 個 | NPC 對局 | 數食物，吃掉一個 | 永遠 3 個；吃掉的當下補一個新的 |
| F-02 | 5 生成位置 | NPC 對局，多吃幾個 | 看新食物位置 | 只出現在房間/走廊的地板上；不在障礙物（含大型怪物第二格）、其他食物上；（1 秒內的）蛇身上也不會出現 |
| F-03 | 3 吃 1 個 +1 | NPC 對局、`能量(0)` | 吃 1 個 | 自己能量條 0 → 1 |
| F-04 | 5 吃食驗證 | 用腳本送 `food_eaten_request`，foodId 對但 headPos 錯 | — | 伺服器 log「位置不符,忽略」，能量不變、不補食物 |
| F-05 | 2.1 能量可視 | 真人對戰 | A 吃食物 | B 畫面的對手能量條同步增加 |

## EN. 能量（規格 3）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| EN-01 | 3 發動門檻 | NPC 對局、安靜 NPC、`NPC閃避(永遠不)`、`能量(1)` | 按直接攻擊 | 攻擊成立（1 點也能打），NPC 變長 1 |
| EN-02 | 3 超額保留 | 同上、`能量(13)` | 按直接攻擊 | 攻擊消耗 10、剩 3（能量條顯示 3）；attack_incoming 的 attackerEnergyUsed = 10 |
| EN-03 | 3 效果強度封頂 | 同上、`能量(15)`、`下次效果(失明)` | 按隨機攻擊 | 持續時間 = 10 × 0.5 = 5 秒（不是 7.5 秒），剩 5 點 |
| EN-04 | 3 能量 0 不能攻擊 | `能量(0)` | 按攻擊 | 「攻擊未成立:no_energy」 |
| EN-05 | 3 無冷卻 | `能量(20)`、`NPC閃避(永遠不)` | 直接攻擊命中後馬上再按直接攻擊 | 第二次立刻成立（直接攻擊沒有冷卻，也不鎖對手） |

## A. 攻擊（規格 4.1、4.3）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| A-01 | 9.2 按鈕互動 | — | 點左鈕、點右鈕；長按任一顆 0.5 秒再放開 | 點擊發動對應攻擊；長按時按鈕變亮、上方出現說明氣泡，放開後氣泡消失且**不發動** |
| A-02 | 4.3 未結算不能連發 | NPC 對局、`能量(20)`、`NPC閃避(正常)` | 按攻擊後立刻再按 | 等結果期間兩顆按鈕都變灰按不下去；（用腳本連送兩次 attack_request → 第二次 attack_rejected: attack_in_progress） |
| A-03 | 4.1a 直接攻擊 | 安靜 NPC、`NPC攻擊(direct, 0, 7)`，自己不閃 | 看自己的蛇 | 約 1 秒後命中，自己變長 7 節（接下來 7 步尾巴不動） |
| A-04 | 4.1b 效果隱藏 | `能量(6)`、`NPC閃避(永遠不)` | 按隨機攻擊，看警示期間 | attack_incoming 裡沒有效果種類；雙方在閃躲視窗期間都看不到是哪種效果，結果出來才顯示 |
| A-05 | 4.3-1 暫停中不能攻擊 | `下次效果(暫停)`、`NPC攻擊(random, 0, 6)`，不閃 | 效果生效後（預告 1 秒之後）按攻擊 | 按鈕沒反應（Flutter 本地擋下）；伺服器若收到回 attacker_paused |
| A-06 | 4.3-3 對方效果中不能打 | `能量(20)`、`下次效果(失明)`、`NPC閃避(永遠不)` | 隨機攻擊命中，等 1 秒預告後，效果期間再按任一攻擊 | 「攻擊未成立:target_effect_active」；直接攻擊也一樣被擋 |
| A-07 | 4.3 註 直接攻擊不鎖 | `能量(20)`、`NPC閃避(永遠不)` | 直接攻擊命中後立刻按隨機攻擊 | 成立 |
| A-08 | 4.3 補充 被失明/加速時可反擊 | `下次效果(失明)`（再做一次換加速）、`NPC攻擊(random, 0, 6)`，不閃；`能量(5)` | 效果生效期間按攻擊 | 可以正常發動 |

## D. 閃避（規格 4.2、7.5）與斷線（6.2）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| D-01 | 2.1、9.1、9.3 警示 | 安靜 NPC、`NPC攻擊(direct, 3000, 3)` | 按住搖桿等 | 3 秒後：畫面外框變紅、蛇頭上方閃爍準星、「⚡ 被攻擊了!放開搖桿閃躲」；直接/隨機兩種攻擊的警示**完全一樣**（Flutter 另有震動，規格寫不震動，見差異 #4） |
| D-02 | 4.2 閃避成功 | 同上 | 警示出現後 1 秒內放開搖桿 | 「閃躲成功!」；沒有效果、沒變長；NPC 那邊記一次被閃 |
| D-03 | 4.2 視窗 + 延遲校正 | 同上 | 警示出現後超過 1 秒才放開（或一直按著） | 閃躲失敗，套用效果。高延遲網路（手機熱點）下在 1 秒內放開仍應判成功（伺服器用 RTT/2 校正） |
| D-04 | 6.2 對手斷線倒數 | 真人 A、B 對戰 | B 強制關閉 App（不是切背景）或拔網路 | A 顯示「對手連線中斷,等待重連... 10」逐秒倒數，A 的蛇停止不動。（只拔網路時 TCP 可能不會馬上斷，伺服器最多 60 秒後由心跳偵測） |
| D-05 | 6.2 寬限內重連 | 同 D-04 | B 在 10 秒內重開 App 連回 | 規格：遊戲繼續、狀態保留。**Flutter 目前預期失敗**：B 重開後回到大廳，沒有回到對戰（見 PARITY 差異 #1）；A 會收到 opponent_reconnected 解除凍結但 B 已不在 |
| D-06 | 6.2 超時判負 | 同 D-04 | B 不回來，等 10 秒 | A 顯示「你贏了!」（opponent_disconnect_timeout） |

## E. 三種負面效果（規格 4.1b）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| E-01 | 4.1b 預告 1 秒 | 安靜 NPC、`下次效果(加速)`、`NPC攻擊(random, 0, 4)`，不閃 | 看閃躲失敗後 | 先顯示「即將發動:speedup 效果」，**1 秒後**才開始加速，持續 4 × 0.5 = 2 秒 |
| E-02 | 加速 | 同 E-01 | 計速度 | 效果期間約 160ms/格，結束後回到 325ms/格 |
| E-03 | 暫停 | `下次效果(暫停)`、`NPC攻擊(random, 0, 4)`，不閃 | 效果期間推搖桿、按攻擊 | 蛇完全不動、搖桿無效、攻擊按不出去，持續 2 秒 |
| E-04 | 4.1b 暫停上限 3 秒 | `下次效果(暫停)`、`NPC攻擊(random, 0, 10)`，不閃 | 計時 | 暫停 3 秒（不是 5 秒）；attack_result 的 effectDuration = 3000 |
| E-05 | 失明 | `下次效果(失明)`、`NPC攻擊(random, 0, 8)`，不閃 | 效果期間轉幾次彎 | 畫面全黑，只看得到蛇頭和它**前方兩格**，轉向後視野跟著換；持續 4 秒 |
| E-06 | 4.1b 效果機率均等 | `NPC閃避(永遠不)`、每次 `能量(2)` | 不指定效果，隨機攻擊 30 次（每次等效果結束） | 三種效果各約 1/3（容許範圍約 5～15 次） |
| E-07 | 6 暫停解除撞身體 | 貼著自己身體繞圈、剛被直接攻擊變長（尾巴暫時不動）時中暫停（先 `NPC攻擊(direct, 0, 10)` 命中，接著 `下次效果(暫停)` `NPC攻擊(random, 0, 6)`） | 暫停結束 | 若原方向正前方是身體 → 死亡 self，伺服器採信。（很難手動湊出來，可視情況跳過或用腳本） |

## P. 防騷擾懲罰（規格 4.4）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| P-01 | 4.4 被閃 3 次自己暫停 3 秒 | 安靜 NPC、`NPC閃避(永遠)` | 每次 `能量(1)` 後按直接攻擊，共 3 次 | 前 2 次「對方閃躲成功」；第 3 次後出現「連續被閃躲,自己暫停3秒」，自己的蛇停 3 秒 |
| P-02 | 4.4 暫停中不能攻擊 | 接 P-01 | 暫停中 `能量(5)` 按攻擊 | 按不出去（伺服器若收到回 attacker_paused）；3 秒後恢復可攻擊 |
| P-03 | 4.4 累計不需連續、命中不重置 | 安靜 NPC | 被閃（`NPC閃避(永遠)`）→ 命中（`NPC閃避(永遠不)`，等效果結束）→ 被閃 → 被閃 | 第 3 次被閃（中間夾一次命中）時觸發自我暫停。觸發後計數歸零：之後要再被閃 3 次才會再觸發（伺服器是「觸發當下」歸零，規格寫「暫停結束後」，見差異 #5） |

## H. 攻擊成功橫幅（Flutter 行為）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| H-01 | 9.3 攻擊動畫卡位 | 真人 A、B，A 能量 5 | A 攻擊、B 不閃 | **雙方**畫面都出現橫幅（右滑入 → 停在中間 → 左滑出，共 2 秒）；閃掉時不出現 |
| H-02 | 命中後雙方停止 | 同上 | 看兩邊的蛇 | 橫幅這 2 秒雙方的蛇都不動，之後繼續 |

## U. 對手資訊與畫面（規格 2.1、7.3、9.1）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| U-01 | 2.1、9.1 能量條 | `能量(0)`→`(4)`→`(7)`→`(10)`→`(12)` | 看自己的能量條；另一台看對手能量條 | 自己：綠（<5）→ 黃（5～9）→ 紅（≥10）且滿量白框，數值正確；對手能量條一律黃色 |
| U-02 | 2.1、7.3 模糊小地圖 | 真人 A、B | A 在地圖角落待著，看 B 的小地圖 | 每 2 秒跳一次，位置在 A 真實位置附近（伺服器實際雜訊 -2～+1 格，見差異 #11），不會精確 |
| U-03 | 7.3 NPC 例外 | NPC 對局 | 看小地圖 | 一直是空的（NPC 沒有座標） |
| U-04 | 9.1 版面 | 手機直向（有瀏海的機型）；Godot 電腦版可開 `simulate_iphone_safe_area`（F2） | 看畫面 | 上：你/能量條、小地圖、對手/能量條；下：左閃電鈕、中搖桿、右骰子鈕，半透明，不被瀏海和底部滑動條擋住 |
| U-05 | 一次性訊息 | 觸發任一訊息（例如 EN-04） | 看訊息 | 上方出現黑底訊息，3 秒後自動消失，也可按 × 關掉 |
| U-06 | 設定 | — | 大廳進設定關掉光影特效 | 對戰畫面沒有火光/景深/暗角，一般清楚畫面 |

## W. 勝負（規格 6、6.1）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| W-01 | 6 單方死亡 | NPC 對局 | 撞牆 | 「你輸了」→ 返回大廳；reason single_death |
| W-02 | 6 對手死亡 | 真人 A、B | B 撞牆 | A 顯示「你贏了!」 |
| W-03 | 6、6.1 Double KO | 真人 A、B | 兩邊同時撞牆（手動很難對準，建議用腳本讓兩個連線在 200ms 內各送一個合法的 death_report；再試一次間隔 >200ms） | ≤200ms：雙方「平手(Double KO)」；>200ms：先回報的輸（sequential_death） |
| W-04 | 主動離開 | 對戰中 | 一方離開對戰（或 App 切背景，見 C-07） | 另一方「你贏了!」（opponent_left） |

## N. NPC 行為（規格 8.2.1）

| ID | 規格 | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|---|
| N-01 | 8.2.1 NPC 吃食物 | NPC 對局 | 看對手能量條 1 分鐘 | 大約每 2～3.5 秒 +1 |
| N-02 | 8.2.1 NPC 出手時機與種類 | NPC 對局（自動攻擊開著），多打幾場 | 記錄 NPC 出手時的能量與攻擊種類 | 能量在 5～10 之間出手（每次門檻不同），直接/隨機大約各半 |
| N-03 | 8.2.1 NPC 閃避 30% | `NPC閃避(正常)`，每次 `能量(1)` 直接攻擊 30 次（注意第 3 次被閃會觸發自我暫停，等 3 秒再繼續） | 數被閃次數 | 約 30%（容許約 4～15 次） |

---

## 除錯工具自檢

| ID | 前置設定 | 操作步驟 | 預期結果 |
|---|---|---|---|
| T-01 | 伺服器**沒設** `SBG_DEBUG_COMMANDS` | 面板連線 → 重新整理 | 沒有任何回應（除錯指令等同不存在），伺服器啟動訊息沒有「除錯指令已啟用」 |
| T-02 | `SBG_DEBUG_COMMANDS=1 NODE_ENV=production npm start` | 同上 | 啟動時印「NODE_ENV=production,拒絕啟用除錯指令」，面板沒有回應 |
| T-03 | Godot 用 release 設定匯出 | 開 App | 左上角沒有 DBG 按鈕，F3 沒反應 |
| T-04 | 除錯模式 | 送一個不合法的值（例如在腳本送 `debug_force_next_effect` effect:"fly"） | 面板顯示紅色 ✗ 錯誤訊息，伺服器狀態不變 |
